[package]
name = "nonogram-graph-search"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
nonogram-core = { path = "../nonogram-core" }
nonogram-propagation = { path = "../nonogram-propagation" }
log = { version = "0.4", features = ["std"] }
